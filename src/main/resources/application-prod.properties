# Production config for Render deployment
# Server
server.port=${PORT:8080}

# Database (Render Postgres)
spring.datasource.url=jdbc:postgresql://${DB_HOST:dpg-d71rnpvdiees73bvmkr0-a.oregon-postgres.render.com}:${DB_PORT:5432}/${DB_NAME:smartpa}
spring.datasource.username=${DB_USER:smartpa_user}
spring.datasource.password=${DB_PASS:CcmhjPIS69jy9k9UAPiQN3AJZAiRpVz3}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
jwt.secret=${JWT_SECRET:SmartPA_Super_Secret_Key_2024_Prasanna_AI_Assistant_Application}
jwt.expiration=86400000

# Groq AI (OpenAI-compatible)
openai.api.key=${OPENAI_API_KEY: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}
openai.model=llama-3.3-70b-versatile
openai.api.url=https://api.groq.com/openai/v1/chat/completions

# CORS (update for production frontend URL)
cors.allowed-origins=https://your-frontend.onrender.com,http://localhost:5173

# Logging (prod level)
logging.level.com.smartpa=WARN
logging.level.root=ERROR
